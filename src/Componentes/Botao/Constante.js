export const tipoBotao = Object.freeze({
  primario: 'primario',
  secundario: 'secundario'
})

// usando o object.freeze fica imposivel de mudar a propriedade do objeto, gerando um r=[erro no navegador.